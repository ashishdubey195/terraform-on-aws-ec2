version: 0.2

env:
  variables:
    TERRAFORM_VERSION: "1.12.2"
    TF_COMMAND: "apply"
  parameter-store:
    AWS_ACCESS_KEY_ID: "/CodeBuild/MY_AWS_ACCESS_KEY_ID"
    AWS_SECRET_ACCESS_KEY: "/CodeBuild/MY_AWS_SECRET_ACCESS_KEY"

phases:
  install:
    runtime-versions:
      python: 3.7
    on-failure: ABORT
    commands:
      - echo "Installing Terraform version $TERRAFORM_VERSION"
      - wget https://releases.hashicorp.com/terraform/$TERRAFORM_VERSION/terraform_${TERRAFORM_VERSION}_linux_amd64.zip
      - unzip terraform_${TERRAFORM_VERSION}_linux_amd64.zip
      - mv terraform /usr/local/bin/
      - terraform --version

  pre_build:
    on-failure: ABORT
    commands:
      - echo "🧪 DEBUG: Running UPDATED buildspec on $(date)"
      - echo "CODEBUILD_SRC_DIR is: $CODEBUILD_SRC_DIR"
      - echo "Directory tree under CODEBUILD_SRC_DIR:"
      - ls -R "$CODEBUILD_SRC_DIR"

  build:
    on-failure: ABORT
    commands:
      - cd "$CODEBUILD_SRC_DIR/22-IaC-DevOps-using-AWS-CodePipeline/Git-Repo-Files/terraform-manifests"
      - echo "Changed directory to terraform-manifests"
      - ls -lrt
      - terraform init -input=false --backend-config=dev.conf
      - terraform validate
      - terraform plan -lock=false -input=false -var-file=dev.tfvars
      - terraform $TF_COMMAND -input=false -var-file=dev.tfvars -auto-approve

  post_build:
    on-failure: CONTINUE
    commands:
      - echo "Terraform execution completed on $(date)"
